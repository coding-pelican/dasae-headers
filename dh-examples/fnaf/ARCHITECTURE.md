# FNAF - Zig Edition: 프로젝트 구조 및 아키텍처

## 📁 파일 구조

```
fnaf-zig/
│
├── fnaf_game.zig          (440줄) - 순수 게임 로직
│   ├── Location enum              - 11개 위치 정의
│   ├── AnimatronicType enum       - 4개 캐릭터
│   ├── GameState struct           - 중앙 상태 관리
│   ├── updateGame()               - 메인 시뮬레이션 루프
│   ├── updateAnimatronic()        - AI 행동 로직
│   └── Query Functions            - 렌더링을 위한 읽기 전용 API
│
├── fnaf_renderer.zig      (420줄) - SDL2 렌더링
│   ├── Renderer struct            - SDL 관리자
│   ├── AssetManager struct        - 텍스처/폰트 로딩
│   ├── renderFrame()              - 메인 렌더 함수
│   ├── renderOfficeView()         - 오피스 렌더링
│   ├── renderCameraView()         - 카메라 렌더링
│   └── processInput()             - 입력 처리 (경계)
│
├── fnaf_main.zig          (120줄) - 애플리케이션
│   ├── Application struct         - 게임 앱 관리
│   ├── run()                      - 메인 게임 루프
│   └── main()                     - 진입점
│
├── build.zig              (40줄)  - 빌드 시스템
├── build.sh                       - 빌드 스크립트
└── README.md                      - 문서
```

## 🏗️ 계층 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                         APPLICATION LAYER                        │
│                         (fnaf_main.zig)                          │
│                                                                   │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  Main Game Loop                                          │    │
│  │  - Frame timing (60 FPS)                                 │    │
│  │  - Input → Logic delegation                              │    │
│  │  - Logic → Renderer delegation                           │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                            ▼         ▼
          ┌─────────────────┴─────────┴─────────────────┐
          ▼                                               ▼
┌──────────────────────────┐                  ┌──────────────────────────┐
│    LOGIC LAYER           │                  │    RENDERING LAYER       │
│  (fnaf_game.zig)         │                  │  (fnaf_renderer.zig)     │
│                          │                  │                          │
│  ┌────────────────────┐  │                  │  ┌────────────────────┐  │
│  │   GameState        │  │◄─────reads──────┤  │   Renderer         │  │
│  │   (Database)       │  │                  │  │                    │  │
│  │                    │  │                  │  │  - SDL Init        │  │
│  │  - phase           │  │                  │  │  - Texture Load    │  │
│  │  - time            │  │                  │  │  - Draw Calls      │  │
│  │  - power           │  │                  │  │  - Font Render     │  │
│  │  - controls        │  │                  │  └────────────────────┘  │
│  │  - animatronics[4] │  │                  │                          │
│  └────────────────────┘  │                  │  ┌────────────────────┐  │
│          ▲               │                  │  │  AssetManager      │  │
│          │               │                  │  │  - Textures        │  │
│  ┌───────┴────────────┐  │                  │  │  - Fonts           │  │
│  │  Update Functions  │  │                  │  └────────────────────┘  │
│  │  - updateGame()    │  │                  │                          │
│  │  - updateAI()      │  │                  │  ┌────────────────────┐  │
│  │  - checkJumpscare()│  │                  │  │  processInput()    │  │
│  └────────────────────┘  │                  │  │  (Boundary)        │  │
│                          │                  │  └────────────────────┘  │
│  ┌────────────────────┐  │                  └──────────────────────────┘
│  │  Query Functions   │  │
│  │  (Read-only API)   │  │
│  │                    │  │
│  │  - getAnimatronics │  │
│  │    AtLocation()    │  │
│  │  - isAnimatronic   │  │
│  │    AtDoor()        │  │
│  │  - getPowerPercent │  │
│  └────────────────────┘  │
└──────────────────────────┘

           ▼
┌──────────────────────────┐
│   PLATFORM LAYER         │
│   (SDL2 / OS)            │
│                          │
│  - Window Management     │
│  - Graphics API          │
│  - Input Events          │
│  - Audio                 │
└──────────────────────────┘
```

## 🔄 데이터 흐름

### 1. 게임 루프 (Main Loop)

```
┌─────────────────────────────────────────────────────────┐
│                    Frame N                               │
│                                                          │
│  ┌──────────────┐    ┌──────────────┐    ┌───────────┐ │
│  │  Input       │───▶│  Update      │───▶│  Render   │ │
│  │  Processing  │    │  Game Logic  │    │  Frame    │ │
│  └──────────────┘    └──────────────┘    └───────────┘ │
│         │                    │                   │      │
│         ▼                    ▼                   ▼      │
│  processInput()      updateGame()       renderFrame()  │
│         │                    │                   │      │
│         ▼                    ▼                   ▼      │
│  GameState ◄────────── GameState ────────▶ GameState   │
│  (modify)               (modify)           (read-only) │
│                                                          │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
                    Frame N+1
```

### 2. 상태 업데이트 흐름

```
updateGame(state, delta_time)
    │
    ├─▶ time.advance(delta_time)
    │       └─▶ hour++, progress++
    │
    ├─▶ power.update(delta_time)
    │       ├─▶ calculate usage rate
    │       └─▶ remaining -= rate * dt
    │
    ├─▶ for each animatronic:
    │       │
    │       ├─▶ timer -= delta_time
    │       │
    │       ├─▶ if timer <= 0:
    │       │       │
    │       │       ├─▶ roll dice (AI level)
    │       │       │
    │       │       └─▶ if success:
    │       │               │
    │       │               ├─▶ attemptMove()
    │       │               │       │
    │       │               │       ├─▶ getNextLocation()
    │       │               │       │
    │       │               │       ├─▶ check camera block
    │       │               │       │
    │       │               │       └─▶ update location
    │       │               │
    │       │               └─▶ reset timer
    │       │
    │       └─▶ update is_at_door flag
    │
    └─▶ checkJumpscareConditions()
            │
            └─▶ if threat: phase = game_over
```

### 3. 렌더링 쿼리 흐름

```
renderFrame(state)  ◄── state (read-only pointer)
    │
    ├─▶ switch state.phase:
    │       │
    │       ├─▶ .active:
    │       │       │
    │       │       ├─▶ if viewing_camera:
    │       │       │       │
    │       │       │       ├─▶ renderCameraView()
    │       │       │       │       │
    │       │       │       │       └─▶ getAnimatronicsAtLocation()
    │       │       │       │               └─▶ [read state.animatronics]
    │       │       │       │
    │       │       │       └─▶ renderCameraStatic()
    │       │       │
    │       │       └─▶ else:
    │       │               │
    │       │               └─▶ renderOfficeView()
    │       │                       │
    │       │                       └─▶ renderDoorControls()
    │       │                               └─▶ [read state.controls]
    │       │
    │       ├─▶ .game_over: renderJumpscare()
    │       │
    │       └─▶ .survived: renderVictory()
    │
    └─▶ renderUI()
            │
            ├─▶ getPowerPercentage()
            │       └─▶ [read state.power.remaining]
            │
            └─▶ getCurrentHour()
                    └─▶ [read state.time.hour]
```

## 🎯 설계 결정 사항

### 1. 왜 단일 GameState 구조체인가?

**문제점 (분산 상태):**
```zig
// ❌ 나쁜 예
var time: GameTime = ...;
var power: PowerState = ...;
var controls: PlayerControls = ...;
// → 동기화 문제, 일관성 없음
```

**해결책 (중앙화):**
```zig
// ✅ 좋은 예
pub const GameState = struct {
    time: GameTime,
    power: PowerState,
    controls: PlayerControls,
    // → 단일 진실 공급원
};
```

### 2. 왜 계산 vs 캐싱인가?

**캐싱 (안티패턴):**
```zig
// ❌ 동기화 문제
pub const PlayerControls = struct {
    left_door_closed: bool,
    right_door_closed: bool,
    power_usage: f32,  // ← 캐시된 값
    power_usage_dirty: bool,  // ← 더티 플래그
};
```

**실시간 계산 (권장):**
```zig
// ✅ 항상 정확함
pub fn calculatePowerUsage(self: PlayerControls) f32 {
    var usage: f32 = 0.1;
    if (self.left_door_closed) usage += 0.1;
    return usage;  // ← 항상 최신 값
}
```

### 3. 왜 예외 없는 내부 로직인가?

**예외 전파 (복잡함):**
```zig
// ❌ 불필요한 복잡성
pub fn updateAnimatronic(state: *GameState) !void {
    if (state.something_wrong) return error.Invalid;
    // → 모든 호출자가 try/catch 필요
}
```

**전제 조건 명확화 (단순함):**
```zig
// ✅ 간단하고 빠름
pub fn updateAnimatronic(state: *GameState) void {
    // 전제: state는 항상 유효
    // 전제: animatronics 배열은 항상 4개
    // → 예외 발생 불가능
}
```

### 4. 왜 로직과 렌더링 분리인가?

**이점:**
1. **테스트 용이성**: 로직만 단위 테스트 가능
2. **재사용성**: 다른 렌더러 (OpenGL, Vulkan) 교체 가능
3. **병렬화**: 로직과 렌더링 멀티스레드 가능
4. **디버깅**: 각 레이어 독립적 검증

```zig
// Logic layer - 렌더링 독립적
pub fn updateGame(state: *GameState, dt: f32) void {
    // 순수 계산, I/O 없음
}

// Renderer layer - 로직 독립적
pub fn renderFrame(renderer: *Renderer, state: *const GameState) void {
    // 상태 읽기만, 수정 없음
}
```

## 📊 성능 특성

### 메모리 레이아웃

```
GameState (컴팩트한 구조):
    phase:         1 byte
    time:          5 bytes  (hour + progress)
    power:         8 bytes  (remaining + rate)
    controls:      5 bytes  (bools)
    animatronics:  96 bytes (4 * 24 bytes)
    night_number:  1 byte
    random_state:  8 bytes
    ──────────────────────
    Total:        ~128 bytes (캐시 친화적!)
```

### 시간 복잡도

```
updateGame():           O(1)
    - time update:      O(1)
    - power update:     O(1)
    - animatronic AI:   O(n) where n=4 (상수)
    - jumpscare check:  O(n) where n=4 (상수)

renderFrame():          O(1)
    - texture copy:     O(1) GPU 작업
    - text render:      O(k) where k=UI elements (작음)
    - query calls:      O(1) or O(n=4)
```

### 최적화 특성

1. **Data-Oriented Design**: 모든 애니매트로닉을 배열로 관리
2. **Cache-Friendly**: 컴팩트한 구조체, 순차 접근
3. **Zero-Cost Abstractions**: inline 함수, 컴파일 타임 최적화
4. **No Allocations**: 게임 루프 중 힙 할당 없음 (문자열 제외)

## 🔮 확장 가능성

### 1. 멀티플레이어 추가

```zig
// 현재 구조는 네트워크 추가에 이상적
pub const NetworkState = struct {
    game: GameState,  // ← 전송 가능
    player_id: u32,
};

// 클라이언트는 GameState만 수신하여 렌더링
// 서버는 GameState만 시뮬레이션
```

### 2. 리플레이 시스템

```zig
// 입력만 기록하면 재현 가능
pub const InputRecord = struct {
    frame: u64,
    action: PlayerAction,
};

// GameState.init(seed)로 동일한 결과 보장
```

### 3. AI 개선

```zig
// AI 시스템 교체 가능 (인터페이스 동일)
pub fn updateAnimatronicML(state: *GameState, anim: *AnimatronicState) void {
    // 머신러닝 기반 AI로 교체
}
```

## 🎓 학습 포인트

이 프로젝트에서 배울 수 있는 개념:

1. **Data-Oriented Design**: 데이터 중심 설계
2. **Separation of Concerns**: 관심사의 분리
3. **Immutable Query Pattern**: 불변 쿼리 패턴
4. **Boundary-Based Error Handling**: 경계 기반 에러 처리
5. **Deterministic Simulation**: 결정론적 시뮬레이션
6. **Cache-Friendly Code**: 캐시 친화적 코드
7. **Type-Safe Abstractions**: 타입 안전 추상화

---

**이 구조는 친구를 놀라게 하기에 충분합니다!** 🎮🐻
